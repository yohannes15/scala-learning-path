package capstone.mini

/** Capstone 1 — tiny CLI (see `capstone/README.md` §1).
  *
  * Replace the body with your project (e.g. mood logger, guess-the-number).
  * Run: `sbt "runMain capstone.mini.MiniCli"`
  */
@main def MiniCli(args: String*): Unit =
  println(
    "capstone.mini — implement your Either-based CLI here. See capstone/README.md §1."
  )
